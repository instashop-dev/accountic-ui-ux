## Why

UI/UX tasks in this project (layout, typography, component design, visual hierarchy, accessibility, responsive behaviour) currently have no explicit skill invocation rule, meaning the `ui-ux-pro-max` skill — which provides structured, expert-level design guidance — is only used if remembered in the moment. Adding a standing rule to CLAUDE.md ensures it is always invoked for any frontend/UI/UX work, producing consistently higher-quality design decisions across every task in this repo.

## What Changes

- Add a new rule to `CLAUDE.md` (Rule 13) directing Claude to invoke the `ui-ux-pro-max` skill before undertaking any task that touches UI, UX, layout, styling, visual design, component structure, or accessibility in this project
- The rule should define the trigger conditions clearly so it is unambiguous when the skill applies

## Capabilities

### New Capabilities

- `ui-ux-skill-invocation-rule`: The CLAUDE.md rule that mandates `ui-ux-pro-max` skill invocation for all UI/UX work in this repo

### Modified Capabilities

## Impact

- `C:\Users\smili\Desktop\CLAUDE.md`: one new rule appended (Rule 13)
- No code changes, no build changes, no other files affected
